---
  - name: レポジトリの追加
    copy: src=nginx.repo dest=/etc/yum.repos.d/nginx.repo owner=root group=root mode=0644
  
  - name: copy test2
    copy: src=nginx.repo dest=/home/ec2-user/nginx.repo owner=root group=root mode=0644

  # - name: nginxのインストール
  #   yum: name=nginx disablerepo=amzn-main enablerepo=nginx state=present
  
  # - name: nginxの初期起動オン
  #   service: name=nginx enabled=yes